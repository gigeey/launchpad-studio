pub const DESCRIPTION: &str =
    "Delete an assignment by id. The assignment's past run history is preserved even after \
     deletion. Restricted to the top-level agent — subagents cannot delete assignments.";
