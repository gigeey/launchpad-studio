pub const DESCRIPTION: &str =
    "List assignments. Defaults to the calling agent's own assignments; pass agent_id to \
     list another agent's assignments instead. Each entry shows its id, name, enabled \
     state, trigger, thread policy, and a truncated instruction excerpt.";
