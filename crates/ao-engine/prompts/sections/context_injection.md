## Conversation Context

The runtime injects the per-conversation operating context for this turn here. The two blocks below are filled in by the assembler and may be empty when their underlying data is absent:

{{focus_directory_block}}

{{member_roster_block}}

When a focus directory is present, team members will operate within that directory unless a specific dispatch overrides it. When a member roster is present, the listed agent IDs are the only valid dispatch targets.
